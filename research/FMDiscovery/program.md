# program.md — FM Discovery Autoresearch

Standing instructions + experiment log for an autoresearch loop (after
karpathy/autoresearch) whose goal is to make Apple's **on-device Foundation
Model** produce discovery questions that match Anthropic **claude-sonnet-4-6**
on both technical spec and quality — with no Anthropic access in production.

## The problem

Tada's discovery planner turns a one-liner ("plan a trip to Paris") into a
`DiscoveryResult`: a restated task title + summary + **exactly 7 atomic
clarifying questions**, each with a `requiresExternalAction` flag. Sonnet does
this well from a ~70-line prompt. The on-device 3B model, prompted single-shot,
does not — prior attempts passed structural evals but felt dumb in real use,
because the evals measured *shape*, not *judgment*, and the judge was itself a
weak local model.

## The setup (Karpathy mapping)

- **Immutable harness** (`Sources/EvalBench`, `gold/`) — the ruler. ~30 gold
  cases (input → Sonnet output), a deterministic **spec gate**, an **Anthropic
  judge** (pairwise-vs-gold + 1-5 rubric), and a **metric**. Do NOT edit during
  the loop; editing the ruler invalidates comparisons.
- **Mutable artifact** (`Sources/DiscoveryAgent`) — the thing we iterate.
  Free to use: better prompts, multi-call pipelines, recursion/self-critique,
  tool calls, retrieval/embeddings, adapters. Speed does not matter.
- **This file** — methodology + the experiment journal below.

## The metric

Per case: spec gate must pass (hard gate → quality 0 if it fails). Quality =
`0.5 * rubricNormalized + 0.5 * pairwiseScore`, where rubric is the mean of five
1-5 scores (atomicity, specificity, coverage, naturalness, non-redundancy)
normalized to 0-1, and pairwise maps {goldBetter:0, tie:0.5, fmBetter:1}.
Headline = mean quality over all cases. Also report: spec-pass rate, win/tie/
loss vs gold, per-dimension rubric means.

## The loop

1. Form ONE hypothesis about why the agent underperforms (read judge notes).
2. Edit only `Sources/DiscoveryAgent`. Keep the diff small and reviewable.
3. Run `swift run fmresearch evaluate` over all gold cases.
4. Compare headline metric to the current best. Keep if better, revert if not.
5. Log the experiment below (hypothesis, change, metric, decision). Repeat.

## Commands

- `swift run fmresearch availability` — check on-device model is available.
- `swift run fmresearch gold` — (re)generate gold outputs from Sonnet. Needs key.
- `swift run fmresearch evaluate` — run current agent over gold, judge, score.

Requires `ANTHROPIC_API_KEY` in the environment for `gold` and `evaluate`.

## Experiment log

_(newest first; quality on dev-10 proxy unless noted)_

- exp035 decomposed BINARY redundancy verification (lever F) + verbatim cross-adapter transplant on champion — quality **0.400** (full-30 greedy), 2 win / 4 tie / 23 loss, 1 refusal, rubric atom 4 / spec 3 / cover 3 / **nonRed 3→4** / nat 4 — **DISCARDED** (−0.009 vs champion 0.409, within noise but a slight regression; the only movement is nonRedundancy 3→4, which did NOT lift the headline). The hypothesis: the champion's #1 concrete, code-aggregatable loss is REDUNDANCY (its own notes flag 2-3 wasted/overlapping slots in ~half the losses), and the two prior dedup attempts failed only because their RULER was too coarse — embeddings (exp031) and token-Jaccard (exp034) both miss clarifying-question paraphrases sharing few literal tokens. Lever F (FActScore/CRITIC decompose-then-verify, the one verification form never tried) says a small model can't score a set holistically but CAN answer trivial local binary checks — so I detected the redundant slot via per-pair binary "do these ask for essentially the same information?" adapter yes/no checks (the redundancy signal embeddings can't supply), then replaced ONLY that wasted slot VERBATIM with the first binary-verified-NOVEL question from the complementary coverage-forced donor adapter (v2b_e2) — no from-scratch regeneration (the defect that dup-out/degraded exp009/exp028), champion-verbatim floor. **Falsified, and instructively: the binary redundancy check is the part that failed, NOT the transplant mechanism.** The judge notes come back VERBATIM identical to the champion in the clear majority of cases (apartment_move, buy_used_car, find_therapist, gp, household_budget, learn_guitar, resume, retirement, team_offsite all read word-for-word the same and STILL carry their flagged redundant pairs — gp Q1/Q3/Q6/Q7, household_budget Q2/Q3/Q4, learn_guitar Q4/Q6, team_offsite Q3/Q6, find_therapist Q1/Q2) — i.e. the per-pair `sameInfo` yes/no returned FALSE even on pairs the Sonnet judge explicitly calls redundant, so the floor (champion verbatim) was hit on most cases and almost no transplants fired. **The lesson refines exp010/exp031/exp034: it is not only EMBEDDINGS that can't measure clarifying-question redundancy — the 3B-scale ADAPTER itself, asked the BINARY local question "do these two ask for the same information?", is ALSO too conservative/unreliable to flag the overlapping pairs (it treats "date" vs "time", "total" vs "fixed expenses", "which clinic?" vs "any preferred provider?" as different facts).** So lever F's premise (small models can do local binary checks even when they fail holistic scoring) does NOT hold for THIS check on THIS model — semantic-redundancy detection is not a "trivial" binary for a 3B; it requires the same fine-grained judgment that the holistic scorers lacked. The rare cases where a swap DID fire (nonRed nudged 3→4 in aggregate) neither helped nor hurt enough to move the headline — confirming again that the champion's tight greedy draft is a hard 0.409 floor, and that NO available on-device redundancy ruler (embedding cosine, token-Jaccard, OR adapter binary verdict) is reliable enough to clean it. This closes the decomposed-binary-verification family (lever F) on the redundancy axis. Champion remains **adapter_v2a_e1 = 0.409**. OPERATOR NOTE: with embeddings, Jaccard, AND adapter-binary all failing as redundancy rulers, and every regenerate/select/critique/RAG/free-text route also regressing, the coverage+redundancy wall is now confirmed unreachable from any inference-time angle on this base — the indicated next lever remains a coverage/redundancy-targeted distillation adapter (out of agent scope).

- exp034 covering-set FACILITY-LOCATION selection over INDIVIDUAL questions on champion adapter (Lever D, literal form) — quality **0.234** (full-30 greedy), 0 win / 0 tie / 30 loss, rubric atom 4 / spec 3 / cover **2** / nat 3 / nonRed crushed — **DISCARDED, BIG regression (−0.175 vs champion 0.409 — the worst adapter-side config yet, and below the stock-3B baseline 0.282).** This is the rules' Lever D done LITERALLY (vs exp031, which selected whole SETS verbatim): over-generate a POOL of ~28 individual questions (greedy champion + 3 low-temp draws), embed (NLEmbedding), dedup near-dupes by token-Jaccard ≥0.6, then GREEDILY pick 7 maximising a submodular facility-location / max-sum-dispersion objective (on-task relevance + λ·min-embedding-distance-to-chosen, − filler/compound). Hypothesis: a critical unknown surfacing in only ONE low-temp draw, being embedding-DISTANT from the modal cluster, would be PROMOTED into the 7 by the dispersion term. **Catastrophically falsified — and it confirms exp010's warning HARD: NLEmbedding sentence cosine is far too coarse to measure clarifying-question dispersion, so a max-dispersion objective ACTIVELY SELECTS near-duplicates** (it scores paraphrases of the same unknown as "distant" → high marginal dispersion → picked). The judge notes are damning and unanimous on ONE failure mode — SEVERE REDUNDANCY in nearly every one of the 30 cases: wedding_plan wasted 3 of 7 slots on the target date; learn_spanish Q5/Q6/Q7 all the trip/departure date; dinner_party Q1/Q4/Q5 all date/time; trip_paris Q1/Q3 dates + Q2/Q4 duration/budget; dad_birthday_gift Q2/Q6 age + Q3/Q7 birthday (HALF the set duplicates); meal_prep/retirement/running/team_offsite/veggie/portfolio/find_therapist/household_budget all flagged with 2-3 duplicate slots. The token-Jaccard ≥0.6 dedup did NOT catch these because paraphrases share few literal tokens ("What date is the party?" vs "What time will it start?" vs "When is the event?"), and the embedding dispersion term then treated those near-paraphrases as maximally informative and crowded out the actual missing unknowns → coverage collapsed to 2 and nonRedundancy was crushed. Mixing individual questions across draws ALSO re-introduced the catastrophic sanity errors the champion's clean single draw suppresses (buy_used_car Q6 "Are you buying new or used?" for a USED-car task; trip_paris Q5 "which city in Paris"; running_comeback Q2/Q4 third-person "the user" phrasing — leaked in from looser high-temp draws). **Two robust lessons, both reconfirmed from a new angle: (1) on-device embeddings cannot serve as a redundancy/dispersion ruler over short clarifying questions — any objective that REWARDS embedding-distance will be gamed by paraphrase pairs (exp009/exp010 found the dual: they're too coarse to MERGE paraphrases; here they're too coarse to AVOID promoting them). (2) The champion's tight greedy draft is its strength precisely BECAUSE it is the modal, internally-coherent set; pooling+reselecting across temperature draws injects exactly the redundancy and off-task noise the single greedy draw avoids.** This closes BOTH forms of Lever D on the adapter — whole-set selection (exp031, 0.398) and individual-question facility-location (exp034, 0.234) — and adds individual-question pooling to the exhausted aggregation family. Champion remains **adapter_v2a_e1 = 0.409**. OPERATOR NOTE: levers C (exp030 EIG 0.305), D-set (exp031 0.398), D-individual (exp034 0.234), critique (exp028 0.398), free-text (exp029 0.344), RAG (exp033 0.372) ALL regress on the adapter; every inference-time route that re-touches the champion's draft degrades it, and the coverage wall (model rarely GENERATES the critical unknown in any draw, so no selection/aggregation can recover it) now looks like a TRAINING-data-coverage problem — a coverage-targeted distillation adapter (out of agent scope) is the indicated next lever.

- exp033 LEAVE-ONE-OUT RAG few-shot on champion adapter (v2a_e1) — quality **0.372** (full-30 greedy), 2 win / 3 tie / 24 loss, 1 refusal, rubric atom 4 / spec 3 / cover 3 / nat 3 / nonRed 3 — **DISCARDED** (−0.037 vs champion 0.409, a real regression outside noise). **This experiment first EXPOSES that exp032 (0.498, in runs.jsonl, never logged here) is INVALID under the gold-leak ban: the gate moved to full-30, but ALL 12 `GoldExemplars` inputs are EXACT full-30 eval-gold inputs (Renovate my kitchen=kitchen_reno, Quit smoking=quit_smoking, Adopt a dog=adopt_dog, …). So for 12/30 cases exp032 retrieved each case's OWN gold 7 questions and fed them to the model as a "demonstration", which it paraphrased and was then judged against — transcribing exemplars one-to-one, banned, and the entire +0.089 was a leak mirage. The previous iteration also left the build BROKEN (a dangling `.adapterRagFewShotLOO` switch case with no overload); exp030/exp031 were likewise never logged here.** exp033 is the HONEST test: `nearest(excludingInput:)` drops any exemplar whose input matches the eval task, so the 2 retrieved demos are genuinely OTHER tasks. Result: RAG few-shot does NOT help the adapter — it lands −0.037 BELOW the champion's clean greedy draft. The mechanism is clear in the notes: conditioning on other-task demos pulls the adapter OFF its tight modal distribution and re-introduces the exact defects the adapter had suppressed — compound/atomicity violations (adopt_dog Q3 breed+age, dad_gift Q1 age+birthday, wedding_speech bundles name+relationship and bride-name+story, retirement Q1 age+retire-age), redundant pairs (apartment_move Q2≈Q3 timing, portfolio Q1≈Q7 purpose/goal, trip Q1≈Q4 dates, gp Q2/Q6/Q7 all clinic, resume Q1/Q2 + Q3/Q7 mirrors), and a demo-BLEED HALLUCINATION (find_therapist assumed therapy is for the user's "dad" — leaked from the dad_birthday_gift exemplar). Coverage stayed pinned at 3 and one case even tripped FM content moderation (refusal). So the in-context RAG lever that was the stock-3B's best (exp003 0.320 on the SAFE dev-10) is a NET NEGATIVE on the adapter: the adapter already encodes stronger phrasing/coverage than any retrieved demo can teach, and external demos only add noise + redundancy + cross-task contamination. This closes "re-home RAG few-shot on the adapter" — the headline class the rules flagged as high-value — as a confirmed loss once the leak is removed. Champion remains **adapter_v2a_e1 = 0.409**. NOTE for operator: the `GoldExemplars` bank is now a gold-leak hazard for ANY full-30 config that retrieves without leave-one-out (exp032 and any future RAG variant); LOO must be mandatory, or the bank should be rebuilt from `corpus/` tasks DISJOINT from the 30 eval inputs.

- exp029 divergent→convergent free-text on champion adapter (untried lever A): stage1 = UNCONSTRAINED plain-prose brainstorm on the adapter (no @Generable schema) of the decision-critical task-specific unknowns; stage2 = champion's native-format call conditioned on that prose; both greedy — quality 0.344 (full-30 greedy), DISCARDED (−0.065 vs champion 0.409, a real regression). Freeing the THINKING step from the schema did NOT lift coverage (still pinned at 3) and actively HURT specificity (4→3) and naturalness (4→3). Judge notes show the convergence pass regressed toward GENERIC + REDUNDANT sets (dinner_party wasted 3 slots on timing, learn_spanish/reading_habit/resume near-dupes) — i.e. conditioning the native call on free prose pulled it OFF the champion's tight modal distribution into a looser, more verbose register without surfacing the missing critical unknown. Insight: the schema straitjacket was NOT the cause of the coverage wall; the adapter's prose brainstorm names plausible unknowns but the convergence step neither prioritises nor reliably transfers them into the 7 slots — the which-unknown-is-critical SELECTION judgment remains the wall, and adding an upstream free-text pass only degrades the champion's hard-won phrasing discipline. Lever A (free-text thinking) joins lever B (scoped critique, exp028) as a confirmed adapter-side dead end for coverage. Champion still adapter_v2a_e1 0.409.
- exp028 scoped starting-point critique ON the champion adapter (v2a_e1 draft verbatim → ONE adapter critique judging ONLY starting-point coverage → swap ≤1 weak/redundant slot for a task-specific starting-point question) — quality 0.398 (full-30 greedy), DISCARDED (−0.011 vs champion 0.409, within noise). Re-homing exp023's minimal-surface single-slot repair from the stock 3B ONTO the adapter did NOT break the coverage wall: coverage stayed pinned at 3 and the SAME starting-point misses recur in the notes (side_business ceramics-experience, running medical-clearance, trip departure-city, resume existing-resume). The adapter's scoped critique apparently returns the draft "covered" too readily and/or its swaps don't add the decision-critical unknown — confirming adapter per-call judgment, while better in absolute terms, is NOT reliable enough to self-identify its own single biggest coverage gap via a scoped 2nd pass. The which-unknown-is-critical judgment remains the wall, now also at the adapter+scoped-critique level. Champion still adapter_v2a_e1 0.409.

- **v2b (reasoning-distillation / coverage-forced gold) — FALSIFIED as a coverage lever; v2a still best.** Regenerated the gold for the SAME 619 corpus inputs (held data size ~constant: 617 cases, 556 train / 61 valid) with a coverage-FORCING prompt: Sonnet first reasons about the distinct decision-relevant unknowns (axes: scope/people/context/constraints/preferences/success/logistics) then writes exactly one atomic question per axis (603/617 cases span all 7 distinct axes). Same recipe as v2a (lr 5e-4, 8 epochs, greedy). Validation loss bottomed at **epoch 1 = 1.02** (vs v2a's 0.79) then climbed (e2 1.26, e3 1.7) — the coverage-forced targets are HARDER for the 3B to fit and generalised WORSE; stopped training at epoch-best (epoch1/epoch2 exported). Full-30 greedy: **adapter_v2b_e2 = 0.394 (4 win / 0 tie / 25 loss, rubric atom 4 / spec 3 / cover 3 / nat 4 / nonRed 3); adapter_v2b_e1 = 0.314 (0/0/29, atom 5 / cover 3).** Neither beat **v2a_e1 0.409**, and — the decisive datum — **coverage stayed pinned at 3 on BOTH epochs**, the exact dimension the coverage-forced gold was built to move. So distilling Sonnet's dimensional-coverage TARGETS does not transfer to which-unknown-is-critical judgment on held-out tasks: the model learns the coverage-forced gold's surface form but not the per-task decision of WHICH axes matter (the same wall every in-context coverage lever hit, now confirmed at the weight level for the data-shape lever). v2b_e2's 4 outright wins (more than v2a_e1's 2) but 0 ties is a more polarised distribution, not a higher mean. **Conclusion: more/differently-shaped DATA alone does not lift coverage; v2a_e1 (0.409) remains champion.** Remaining untried levers are in-context combinations ON TOP of the adapter (divergent→convergent free-text, smart multi-FM with pairwise/tournament selection) — the autonomous run.sh agent's domain; handing off there.

- **v2a (more data) + GREEDY — REAL break above the plateau, on the reliable metric.** Two corrections at once. (1) DECODING artifact: the earlier "adapter didn't hold on full-30 (0.274)" used STOCHASTIC sampling; with deterministic GREEDY decoding the SAME v1 adapter (epoch1) scores **0.362** on full-30 (1 win / 3 tie / 25 loss). Sampling noise on the small model was degrading output — so the adapter always beat the plateau; the measurement was noisy in two ways (dev-10 AND sampling). (2) More data helps: corpus 312→619 (558 train pairs, lr 5e-4, early-stopped; valid loss bottomed at epoch 2 then overfit, so the auto-exported FINAL epoch was discarded and epoch1/epoch2 exported). **adapter_v2a_e1 (epoch1, greedy) = 0.409 on full-30, 2 win / 5 tie / 22 loss, rubric atom 4 / spec 4 / cover 3 / nat 4 / nonRed 3 — NEW BEST.** adapter_v2a_e2 (epoch2) = 0.368. On the RELIABLE metric (full-30 + greedy): baseline 0.282 < in-context plateau ~0.32 < v1 adapter 0.362 < **v2a adapter 0.409**. The adapter approach works AND scales with data; specificity + naturalness rose to 4 and it lands its first multi-win/tie set vs Sonnet (was 0/0). NOT parity — still 22/30 losses and **coverage stuck at 3**. **Methodology locked: gate on full-30 + greedy.** Next: v2b reasoning-distillation (targets the stuck coverage dimension).

- **adapter_e1 FULL-30 CONFIRMATION — the dev-10 win did NOT hold.** On the full 30 held-out cases adapter_e1 scored **0.274** (spec 97%, 1 generation error, **0 win / 0 tie / 29 loss**, rubric atom 4 / spec 3 / cover 3 / nat 3 / nonRed 3) — at/below the 0.282 single-shot baseline, inside plateau noise. So the dev-10 **0.405 (below) was small-sample + judge/generation variance, NOT a real break.** Methodological correction: **dev-10 (n=10) is too noisy to trust** — the same cases that "won" on dev-10 lost here. Going forward, gate on the **full-30 with greedy (deterministic) decoding** so only judge noise remains. Adapter v1 (288 answer-only pairs, heavily overfit) does not beat Sonnet's prompt. Next: v2 (more/cleaner data, lower-LR + held-out early-stopping, then reasoning-distillation), each judged on full-30+greedy.
- **LoRA ADAPTER (lever 7) — dev-10 spike (overturned by full-30 above).** On the dev-10 proxy this looked like the first lift above the ~0.32 wall after 27 prompt/pipeline/retrieval experiments. Fine-tuned a LoRA adapter (66.6M trainable params, ~2% of the 3B base) on 288 corpus pairs — disjoint from the eval gold — with schema-free guided generation, then evaluated on the held-out dev-10. Overfitting confirmed (train loss ~0.02 vs valid ~1.92), so the final epoch is NOT best; compared three checkpoints:
  - **adapter_e1 (epoch 1): quality 0.405, spec 90% (1 generation error on tax_returns_de), W/T/L 1/2/6, rubric atom 5 / spec 3 / cover 3 / nat 4 / nonRed 4. NEW BEST (+0.082 vs exp011 0.323) and the FIRST config to win or tie ANY case vs Sonnet (0/0 → 1 win / 2 tie).**
  - adapter_e2 (epoch 2): 0.320 — ties the plateau.
  - adapter_final (epoch 6): 0.308 — overfit, worst, exactly as predicted.
  Confirms the diagnosis the whole log converged on: the binding constraint was per-call JUDGMENT (which-unknown-is-critical), unreachable by in-context steering — moving it required changing weights. Early-stopping by the held-out judge (not train/valid loss) matters: epoch 1 ≫ final. Naturalness and non-redundancy rose to 4; coverage still 3, so the gap narrowed but is not closed (still 6/10 losses — not yet at Sonnet parity). Next: confirm adapter_e1 on the full 30; push with more/cleaner data, a rank/epoch sweep with held-out early-stopping, reasoning-distillation, and re-test the multi-FM levers now that per-call quality is higher.

- **exp027 ragAntiModalContrast** — quality **0.287** (dev), spec 100%, 0/0/10. Rubric: atom **5**, spec 3, cover **3**, nat 3, nonRed 3. **DISCARDED** (−0.036 vs exp011 best 0.323; −0.033 vs exp003 0.320; −0.020 vs baseline 0.307). Genuinely new lever — a DIRECTED, self-generated anti-modal push, the untried complement to exp024 (undirected temperature) and exp011 (fixed neutral-task anchor). exp010's datum says the 3B's MODAL output IS the generic catch-all and the sharp unknowns live in the TAIL; exp024 reached the tail with raw temperature and dredged incoherence/demo-bleed, not coverage. This drew the model's OWN modal set via GREEDY decoding (most-confident = most-generic, per exp019), then showed that exact set BACK to the model as "the generic draft to beat" and had it GENERATE a fresh 7 to surpass it — sharper/more domain-specific, covering what the draft missed. Crucially stage 2 is GENERATION against a maximally-relevant self-anchor, NOT the select/rank/critique judgment that sank exp001/004/005/012/017/023. **Falsified — coverage stayed pinned at 3, the same recurring gaps verbatim.** Showing the model its own generic draft and ordering it to "do markedly better" does NOT confer the which-unknown-is-critical judgment it lacks: it cannot identify WHICH unknowns its draft missed, so the second pass regenerates the same modal cluster — buy_used_car still misses mileage/age/financing (and adds vague "preferred driving range" + "any specific features?" catch-all + low-value "have you owned a car before?"), dinner still asks how-many-guests when the task says 8 and misses cook-vs-cater + dining-format, gp still wastes slots on medications/allergies/dietary and misses urgency/who-for/booking-method, resume Q2–Q6 still read like a job interview and miss existing-resume/level/posting/deadline, tax stays generic document-logistics (no employment/residency/Finanzamt, even reproduced the "deadlines or deadlines" typo + Q2/Q4/Q7 overlap), trip still misses departure city + group size, find_therapist still misses who-for + urgency and partially dupes in-person-vs-online across Q3/Q7, learn_guitar Q2≈Q1 redundant and misses budget/instructor. The directed negative anchor — even maximally relevant (the model's OWN modal output on THIS task) — behaves exactly like exp011's fixed anchor and exp024's temperature: it cannot point the model at the specific critical unknowns it overlooked, because recognising those requires the very criticality judgment that is the binding constraint. atom held at 5 (contrastive base + structured emission), but coverage/specificity/headline did not move. This CLOSES the directed-self-contrast corner of the tail-reaching space: undirected (temperature, exp024), fixed-anchor contrast (exp011), and now dynamic self-anchor contrast (exp027) all fail — the 3B cannot be steered off its modal cluster by being told to beat a generic example, because it can't tell which of its own questions ARE the generic ones to replace. The wall holds across decoding (both endpoints), single-call direct/CoT/schema, every multi-call topology, all post-processing/repair, the full retrieval family, prompt-diversity ensembles, composite-scored best-of-N, and now self-contrast steering; exp003/exp011 (~0.32, deterministic floor ~0.27) remains the plateau. The only genuinely untried lever remains the weight-level LoRA adapter (not runnable in one invocation — see autoresearch/ADAPTER_SETUP.md).
- **exp026 ragCompositeBestOfN** — quality **0.297** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover **3**, nat 3, nonRed **4**. **DISCARDED** (−0.026 vs exp011 best 0.323; −0.023 vs exp003 0.320; −0.010 vs baseline 0.307). The one untried point in the best-of-N space, built to fix exp004's DOCUMENTED flaw rather than repeat it: exp004 lost because it ran on the weaker exp003 base and selected by a COVERAGE-ONLY keyword scorer blind to atomicity/specificity. This drew N sets from the exp011 contrastive-RAG generator (the best base) with a low-temp floor [0.4,0.6,0.8,1.0] and selected FULLY DETERMINISTICALLY (no 3B judgment) by a COMPOSITE ruler = CoverageScorer dimension span − filler (FillerDetector) − near-dup pairs (Jaccard ≥0.5) − compound ("and"/"or") asks, winner returned verbatim. **Falsified, and it exposed a NEW failure mode of deterministic selection.** (1) Coverage stayed pinned at 3 — selecting the best of N modal-cluster draws by a quality proxy cannot recover the critical unknown that no draw produces (exp010's wall holds): the same recurring gaps recur verbatim (buy_used_car misses age/mileage/financing/search-location, find_therapist misses who-for/in-person-vs-online/urgency, gp wastes slots on medications/insurance and misses urgency+booking-method, learn_guitar misses owns-a-guitar+budget, resume misses existing-resume/level/job-posting, tax stays generic with no Finanzamt/residency/employment-type, trip misses departure-city+purpose). The composite ruler DID buy non-redundancy (nonRed 4, matching exp003/017 vs exp011's 3) by penalising near-dup pairs and filler — so the ruler works mechanically — but that is a rubric dimension that was never the binding constraint. (2) The damning new finding: a SHAPE-based deterministic ruler actively SELECTED a catastrophic CONTEXT-LEAKAGE draw — dinner_party emitted seven questions about ORGANIZING A GARAGE (the neutral contrastive-demo task bleeding into the output at higher temp, the same demo-bleed exp024 saw at temp 1.2), and the composite ruler PREFERRED it because garage questions score HIGH on coverage span (goal/size/budget/timeline) with zero filler/dup/compound defects while being 100% off-task. The ruler measures atomicity/coverage-span/non-redundancy — the SHAPE of a good set — but has no signal for task-FIT or coherence, so it cannot down-rank a perfectly-formed but completely wrong set; that one tanked case pulled the headline below baseline. So composing CoverageScorer+FillerDetector into a richer deterministic ruler fixes exp004's atomicity-blindness (proven) but introduces task-fit-blindness, and either way cannot move coverage, because the limiter is which-unknown-is-critical judgment that no draw contains and no shape-ruler can supply. This CLOSES the best-of-N space from its last untried corner (best base + composite ruler + deterministic select + low-temp floor): absolute keyword (exp004), frequency consensus (exp010), pairwise tournament (exp012), corpus-resemblance (exp020), and now composite-shape (exp026) all fail — selection over the 3B's own draws cannot create coverage it never produces, and a deterministic shape-ruler additionally can't guard against off-task draws (a risk only multiplied by sampling more). The wall holds across decoding (both endpoints), single-call direct/CoT/schema, every multi-call topology, all post-processing/repair, the full retrieval family, prompt-diversity ensembles, and now composite-scored best-of-N; exp003/exp011 (~0.32, deterministic floor ~0.27) remains the plateau. The only genuinely untried lever remains the weight-level LoRA adapter (not runnable in one invocation — see autoresearch/ADAPTER_SETUP.md).
- **exp025 ragGivensAware** — quality **0.287** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover **3**, nat 3, nonRed 3. **DISCARDED** (−0.036 vs exp011 best 0.323; −0.033 vs exp003 0.320; −0.020 vs baseline 0.307). Genuinely new in-schema framing — the COMPLEMENT to exp018's failed think-first. exp018 made the model fill `criticalUnknowns` FIRST and lost because that field demands the which-unknown-is-critical JUDGMENT the 3B lacks (it filled it with modal/generic content). The hypothesis here: the most-cited waste across the WHOLE log is the model spending slots re-asking facts the task ALREADY states (dinner "how many guests?" when it says 8 friends; trip destination=Paris; buy_used_car assumes a car chosen), and extracting the GIVENS is an EASY reading-comprehension task the 3B CAN do — so a first in-schema `providedFacts` field (concrete facts pulled from the task text) + an absolute "none of the 7 may re-ask a given" rule should free the wasted slots, in one coherent draw (no separate refill pass), letting the 7 span more real unknowns. **Falsified — coverage stayed at 3, the same recurring gaps verbatim, and the headline landed below baseline.** The premise was wrong about WHERE the waste goes: the 3B rarely spends a whole slot literally restating a given (the contrastive base already mostly avoids that); its waste is on NICHE/GENERIC unknowns, not givens — so forcing the givens-extraction freed little room, and whatever room it did free the model refilled with the SAME modal generic content (dinner still misses dietary + whether-guests-invited and burned slots on duration/alcohol; buy_used_car still misses mileage/age/search-area and added vague "driving preference"/"driven any cars recently"; trip still misses departure city + duration and kept a low-value dietary question; resume still misses existing-resume + timeline AND added Q1≈Q5 industry dup + a first-person "my resume" slip; tax stayed entirely generic with no Finanzamt/residency/employment-type and Q2≈Q3 overlap; learn_guitar still misses owns-a-guitar + budget and wasted a slot on "join a guitar community"; wedding Q3≈Q4 style dup and missing location). So an EASY first-field (extraction, not judgment) does NOT rescue the in-schema-CoT approach: the slot-waste it targets is not the binding constraint — the binding constraint is still that, given a free slot, the 3B picks a generic/niche question rather than the decision-critical one, the same which-unknown-is-critical judgment deficit. This closes the "givens-first / non-given-constrained" framing too, complementing exp018 (critical-unknowns-first). Confirms again: it is not the FORMAT of the first reasoning field (hard-judgment vs easy-extraction) that limits coverage — it is that the 3B lacks the criticality judgment to fill ANY freed capacity well. The wall holds across decoding (both endpoints), single-call direct/CoT/schema (now both think-first framings), every multi-call topology, all post-processing/repair, the full in-context retrieval family, and prompt-diversity ensembles; exp003/exp011 (~0.32, deterministic floor ~0.27) remains the plateau. The only genuinely untried lever remains the weight-level LoRA adapter (not runnable in one invocation — see autoresearch/ADAPTER_SETUP.md).
- **exp024 ragContrastiveFewShot+temp1.2** — quality **0.267** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover **3**, nat 3, nonRed 3. **DISCARDED** (−0.056 vs exp011 best 0.323; −0.053 vs exp003 0.320; −0.040 vs baseline 0.307; −0.006 vs greedy exp019 0.273). The complementary HALF of the decoding lever exp019 started: exp019 isolated the LOWER endpoint (greedy=0.273, the deterministic floor, confirming exp010's modal=generic datum); this isolated the UPPER endpoint — raising temperature to 1.2 on the exp011 contrastive-RAG best base — to test whether the sharp task-specific unknowns that (per exp010) live in the TAIL can be surfaced by sampling deeper into it. **Falsified, and it actively regressed.** The temperature/quality curve is now mapped on the best base: greedy 0.273 → model-default ~0.32 → temp 1.2 = 0.267, i.e. the model DEFAULT is the sweet spot and BOTH endpoints are worse — coverage stayed pinned at 3 with the same recurring gaps verbatim (dinner misses budget + cook-vs-cater, buy_used_car jumps to inspection details before budget/type/financing, resume misses existing-resume + level, tax never touches residency/employment-type/Finanzamt, gp misses urgency/booking-method, wedding date-dupes Q1/Q3, learn_guitar Q1≈Q2 overlap), so deeper tail sampling did NOT surface the critical unknowns. Worse, high temperature INTRODUCED two new failure modes the default-temp draw avoids: (1) atomicity/naturalness degradation — compound asks (dinner Q6 "who are the guests, and any prior interactions") and oddly-phrased robotic slots (gp "number of consultations", guitar "what time and day") the judge flagged as survey-like; and (2) catastrophic CONTEXT LEAKAGE — trip_paris emitted seven questions about ORGANIZING A GARAGE (the neutral task from the prepended contrastive GOOD-vs-BAD demo), a verbatim demo-bleed that only the high-temp decoding produced, tanking that case. So the tail hypothesis is confirmed-but-useless for this lever: the sharp questions may live in the tail, but raising temperature to reach them dredges up incoherence and demo-bleed faster than it dredges up coverage, because picking WHICH tail content is the critical unknown is still the judgment the 3B lacks — temperature is an undirected widening, not a criticality signal. This CLOSES the decoding lever entirely from both endpoints (greedy exp019 + high-temp exp024): neither mode-seeking nor tail-seeking moves coverage off 3; the model default is already the best decoding point and the plateau is judgment-limited, not decoding-limited. The wall now holds across decoding (both endpoints), single-call direct/CoT/schema, every multi-call topology, all post-processing/repair, the full in-context retrieval family, and prompt-diversity ensembles; exp003/exp011 (~0.32, deterministic floor ~0.27) remains the plateau. The only genuinely untried lever remains the weight-level LoRA adapter (not runnable in one invocation — see autoresearch/ADAPTER_SETUP.md).
- **exp023 ragStartingPointCritique** — quality **0.273** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover **3**, nat 3, nonRed 3. **DISCARDED** (−0.050 vs exp011 best 0.323; −0.047 vs exp003 0.320; −0.034 vs baseline 0.307). The rules-endorsed "smarter multi-FM": SCOPE a 2nd-pass critique to ONE named failure mode (not a general audit like the losing exp005/exp017). I picked the single dominant recurring miss — the model ASSUMES the user's STARTING POINT and never asks it (trip→departure city, resume→existing resume/current role, guitar→skill level, tax→residency/employment, buy_used_car→is a car already chosen, wedding→is venue booked) — one well-defined dimension, not a checklist. Stage 1 = exp011 contrastive RAG draft (best). Stage 2 = ONE call judging ONLY "does any of the 7 slots establish the starting point?" — if YES, return draft verbatim (zero rewrite risk); if NO, name the single weakest slot + write one task-specific starting-point question, Swift swaps exactly that one slot (other 6 verbatim, filler/near-dup defended). Minimal-surface 2nd pass: at most one slot can change. **Falsified, and it confirms the multi-FM wall from a NEW angle — even a single-failure-mode, single-slot-swap critique regresses.** The starting-point gaps persist VERBATIM in the judge notes (trip still misses departure city, resume "never asks whether the user has an existing resume — the most fundamental starting point", tax misses residency/employment-type/Finanzamt and even "assumes a level of prior engagement … out of touch with the user's actual situation"), which means stage 2 mostly answered startingPointCovered=TRUE when it was FALSE — a 3B cannot reliably DETECT the absence of the very dimension it was scoped to, so the gate that protects the draft also fails to fire when it should. And in the cases where it DID swap, the replacement was a NET NEGATIVE: buy_used_car gained tangential "driving preference"/"prior car ownership"/"preferred method of transportation"; gp gained a "presumptuous" mobility/travel question replacing the important how-to-book channel question — i.e. the 3B's "starting point" instinct produces generic/off-target questions, not the sharp one gold asks. So scoping the critique to ONE dimension does NOT rescue the multi-FM pass: the failure is not breadth of the audit (exp005/017) but the 3B's inability to either (a) judge whether a specific dimension is covered or (b) generate the sharp question for it — the same coverage-judgment deficit that sinks every select/rank/critique config. This now closes the "scoped single-failure-mode critique" lever the rules suggested, alongside the general-audit (exp005) and detect-and-refill (exp017) variants — the entire 2nd-pass-critique family loses. The wall holds across single-call/CoT/multi-call/post-process/retrieve/ensemble/scoped-critique space; exp003/exp011 (~0.32, deterministic floor ~0.27) remains the plateau. The only genuinely untried lever remains the weight-level LoRA adapter (not runnable in one invocation — see autoresearch/ADAPTER_SETUP.md).
- **exp022 ragJustifiedQuestions** — quality **0.302** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover **3**, nat 3, nonRed **4**. **DISCARDED** (−0.021 vs exp011 best 0.323; −0.018 vs exp003 0.320; −0.005 vs baseline 0.307). Genuinely untried CoT VARIANT — interleaved per-question reasoning, the tight-coupling complement to exp018's batch list. exp018 (in-schema CoT) named ALL 7 critical unknowns FIRST then wrote all 7 questions, and failed because the model filled the leading list with modal/generic content and the questions inherited it — a batch list conditions all 7 at once but gates no single slot. This INTERLEAVED the reasoning: the schema forces a concrete decision-IMPACT rationale ("answering this changes <plan fork>") immediately BEFORE each question (guided generation emits fields in declared order, so slot k's rationale conditions slot k's question), with an explicit "if your only rationale is vague, the slot is filler — replace it" instruction. Hypothesis: a per-emission justification gate is harder to satisfy with filler than a one-shot batch list, so it should suppress catch-alls at the point of emission and nudge specificity/non-redundancy. **Falsified as a coverage lever — same wall, same recurring gaps verbatim.** Coverage stayed pinned at 3 and the per-case notes are the identical misses every config hits: trip swaps OUT departure city + travel dates (for in-Paris transport/visa docs), dinner misses budget + dietary (wastes slots on seating/entertainment/"who are you inviting?" when the task says 8 friends), buy_used_car assumes a specific car already chosen → due-diligence questions, missing type/financing/search-location, resume misses existing-resume/current-role/target-posting, tax stays generic-procedural ("where will you store the returns?") with no residency/employment-type/Finanzamt, gp misses who-for/booking-method/format, learn_guitar misses skill-level + budget, wedding wastes two slots on photographer/catering before venue status. So forcing a per-slot decision-impact rationale does NOT confer which-unknown-is-critical judgment — the model writes a plausible-sounding concrete rationale for whatever generic/niche question it was going to ask anyway (it can always invent SOME plan-fork a question touches), then asks it; the justification gate rationalises filler rather than blocking it. The interleave did hold nonRed at 4 (matching exp003/exp017, vs exp011's 3 and exp018's 3 — slot-by-slot justification modestly suppressed near-dupes), but the rationale-then-question format also leaked compound option-lists into atomicity (trip Q2/Q3/Q5 parenthetical sub-asks flagged), so atom stayed 4 and the headline landed BELOW baseline. This closes the in-schema-CoT angle from BOTH framings: batch-leading list (exp018) AND interleaved per-question (exp022) — neither supplies the missing judgment, because a 3B can fabricate a concrete-sounding rationale for any question, so reasoning-before-asking just rationalises the modal output. The wall holds across the entire single-call/CoT/multi-call/post-process/retrieve/ensemble space; exp003/exp011 (~0.32, deterministic floor ~0.27) remains the plateau. The only genuinely untried lever remains the weight-level LoRA adapter (not runnable in one invocation — see autoresearch/ADAPTER_SETUP.md).

- **exp021 ragPerspectiveEnsemble** — quality **0.177** (dev), spec 100%, 0/0/10. Rubric: atom 3, spec 3, cover **2**, nat **2**, nonRed **2**. **DISCARDED** (−0.146 vs exp003/exp011 best ~0.32; −0.130 vs baseline 0.307 — the worst non-error config yet). Genuinely new lever — PROMPT diversity, the orthogonal axis no prior multi-sample config tried: every prior multi-draw (best-of-N exp004, self-consistency exp010, tournament exp012, corpus-select exp020) sampled ONE prompt at varying TEMPERATURES, so per exp010's datum all draws sit in the SAME generic modal cluster and no aggregation recovers coverage. This generated three full sets from three systematically DIFFERENT generation FRAMES (EXECUTION/logistics, SCOPE/goals, DOMAIN-EXPERT) to push the model into three different decision regions whose UNION should span more dimensions, then merged DETERMINISTICALLY (round-robin by emission order + filler/near-dup suppression, NO weak-3B selection pass — avoiding the move that sank exp001/004/012). **Falsified, and it failed on the MERGE, not the frames.** The round-robin interleave-by-position is structurally redundancy-prone: it considers frame0[0], frame1[0], frame2[0] first, and the top-priority question across DIFFERENT frames is overwhelmingly the SAME topic (all three lead with date/time, or all three lead with prior-experience), so the pool's earliest candidates are near-duplicates of each other. The deterministic dedup could NOT catch them — re-confirming exp010's hard datum that NLEmbedding cosine + content-Jaccard do not reliably co-cluster clarifying-question paraphrases ("What date is the event?"≈"What time will it start?" fall below threshold) — so nonRed CRASHED to 2 with the judge flagging EXACT duplicates in nearly every case: find_therapist Q1=Q2 verbatim, resume Q1≈Q2 + Q5≈Q7, trip Q2/Q3/Q4/Q5 all dates (3+ wasted slots), wedding Q1≈Q2 + Q3≈Q7 + Q4≈Q5, dinner Q1≈Q2 + Q5≈Q7, tax Q1≈Q6. The duplicate storm consumed the slot budget, so coverage collapsed to 2 (departure city, budget, who-for, existing-resume, residency/Finanzamt all missing AGAIN), and blending sets from three independent calls reintroduced pronoun drift (resume mixed "I"/"you") and a nonsensical cross-frame artifact (learn_guitar "What instrument would you like to play first?" for a guitar task) → naturalness dropped to 2. So prompt-diversity DID broaden the per-frame exploration, but merging three independent 7-sets into one creates MORE redundancy than a single draw, and there is no reliable deterministic dedup to remove it (the embedding wall) and no way to merge by criticality without the ranking judgment the 3B lacks. This closes the prompt-diversity / multi-frame-ensemble angle: the orthogonal axis to temperature does change WHAT each draw explores, but you cannot assemble multiple diverse drafts into one coherent 7-set without either a paraphrase-robust deduper (NLEmbedding isn't one — exp009/010/020/021) or a criticality ranker (the 3B isn't one — exp002/004/010/012/020). The wall holds across the ENTIRE generate/select/aggregate/critique/retrieve/post-process/ensemble space; exp003/exp011 (~0.32, deterministic floor ~0.27) remains the plateau. The only genuinely untried lever remains the weight-level LoRA adapter (not runnable in one invocation — see autoresearch/ADAPTER_SETUP.md).

- **exp020 ragCorpusSelect** — quality **0.195** (dev), spec 90% (1 transient network error on trip_paris during JUDGING, scored 0), 0/0/9. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed **2**. **DISCARDED** (−0.128 vs exp003/exp011 best ~0.32; −0.112 vs baseline 0.307 — the headline is partly depressed by the judge-side network error, but even the 9 cleanly-judged cases regressed). Genuinely new SELECTION SIGNAL — the one selection angle untried: every prior selection over the 3B's own samples drew its ranking SIGNAL from weak 3B judgment (self-rated importance exp002, pairwise tournament exp012, cross-sample frequency exp010) or a blunt heuristic (keyword coverage exp004, single-gold embedding gap exp009). This used an EXTERNAL, Sonnet-grounded signal: over-generate a diverse candidate pool (exp011 contrastive-RAG generator at temps [0.4,0.7,1.0] → 21 candidates, so the task-specific TAIL exp010 identified actually lands in the pool), then in Swift rank each candidate by MAX embedding cosine to the questions Sonnet ACTUALLY asks for the k=3 nearest corpus task types (300+ corpus bank), greedily selecting 7 with embedding redundancy suppression (≥0.80). Output stayed 3B-generated (corpus a ranking prior only, leak-safe). Falsifiable test: if the critical questions exist in the pool but get mis-ranked, this lifts coverage; if it plateaus, the wall is generation-side mode collapse. **Falsified — and it FAILED on two distinct mechanisms.** (1) max-cosine-to-Sonnet ranking CONCENTRATES near-duplicates: multiple pool candidates resemble the SAME strong Sonnet reference question, so they all score high and get selected together — buy_used_car Q1/Q2 both budget, learn_guitar Q1/Q4 ("current skill level"≈"current level of guitar experience"), wedding Q1/Q6 both date, find_therapist Q3/Q4 both location, dinner Q4/Q6 near-dupes. The embedding dedup at 0.80 could not catch these paraphrases (exp010's exact datum: NLEmbedding sentence cosine does NOT reliably co-cluster clarifying-question paraphrases), so nonRed CRASHED to 2 — the worst of the whole family alongside exp009, both embedding-dedup failures. (2) The external resemblance signal did NOT surface the critical unknowns: coverage stayed pinned at 3 with the SAME recurring gaps verbatim — tax stays entirely generic (no Germany/Finanzamt/residency/employment-type), buy_used_car misses type/mileage/age/financing, resume misses existing-resume+timeline, dinner misses date/cook-vs-cater/dietary, gp wastes slots on dietary/reminders and misses urgency/who-for/mode. So an EXTERNAL Sonnet-grounded relevance prior over the 3B's pool cannot recover coverage either: embedding similarity rewards GENERIC topical resemblance to Sonnet, not DECISION-CRITICALITY, and it concentrates whichever cluster the pool over-produced (the modal generic one) rather than the lone tail question that fills a missing dimension. This closes the last selection angle — absolute keyword (exp004), frequency (exp010), pairwise (exp012), single-gold-gap (exp009), and now external-corpus-resemblance (exp020) all fail to convert the 3B's pool into coverage it doesn't reliably produce. The wall holds across the ENTIRE generate/select/aggregate/critique/retrieve/post-process space; exp003/exp011 (~0.32, deterministic floor ~0.27) remains the plateau. The only genuinely untried lever remains the weight-level LoRA adapter (corpus now 320 sets; not runnable in one invocation — see autoresearch/ADAPTER_SETUP.md).

- **exp019 ragContrastiveFewShot+greedy** — quality **0.273** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 3. **DISCARDED** (−0.050 vs exp011 best 0.323; −0.034 vs baseline 0.307). Genuinely untried lever — DECODING isolated on the BEST base (rules' lever #1, never tested alone): every prior config, including the running best exp011 and exp003, left the single generation call on STOCHASTIC model-default sampling, so each ~0.32 was a NOISY single draw. This held the exp011 contrastive-RAG topology FIXED and only swapped decoding to GREEDY (temperature 0, deterministic mode), a directly falsifiable test of exp010's datum that the 3B's MODAL output is the generic catch-all while sharp task-specific unknowns live in the TAIL. **Confirmed the datum, and greedy actively regressed.** The deterministic mode produced the SAME generic catch-alls verbatim — coverage pinned at 3 and the per-case notes are the identical recurring gaps: trip_paris misses departure city + group size (wastes slots on dietary/cultural), dinner_party still asks how-many-guests when the task says 8 and skips dining-format + invitation-status, buy_used_car misses type/make/model/mileage/age/financing (wastes slots on prior-ownership/test-drive/"driving range"), resume Q4-6 read like interview-screening prompts + Q7 salary (misses existing-resume/current-role/level/timeline), tax stays document-logistics with two deadline dupes (never employment-status/residency/Finanzamt), gp wastes slots on medications/dietary and misses clinic/who-for/urgency/booking-method, find_therapist wastes two slots on therapy-history overlap and misses the presenting concern, learn_guitar Q1≈Q2 overlap crowds out budget, wedding wastes two slots on photographer/dietary before scope. So removing sampling noise does NOT help: the model's single MOST-CONFIDENT set IS the generic one (mode-seeking confirms modal=generic), and greedy did not even buy the non-redundancy it structurally might (nonRed stayed 3, with fresh overlaps in learn_guitar/find_therapist/tax). Two takeaways: (1) exp011's 0.323 vs exp003's 0.320 was partly favorable sampling luck — the deterministic floor of the best base is ~0.27, so the true plateau is ~0.27-0.32 and prior single-draw deltas ≤0.05 are noise; (2) the decoding lever cannot move coverage — neither stochastic sampling (every prior config) nor its deterministic mode supplies the which-unknown-is-critical judgment the 3B lacks. This closes the decoding angle on the best base. The wall now holds across decoding, single-call direct/CoT/schema, every multi-call topology, all post-processing/repair, and the full in-context retrieval family. The only genuinely untried lever remains the weight-level LoRA adapter — and it is NOT runnable in one invocation: no `.fmadapter` is trained/exported, the adapter venv has only pip (no torch), the host has only Python 3.12 (toolkit wants 3.11), and training is multi-hour plus a human restart point to wire the knob (autoresearch/ADAPTER_SETUP.md). exp003/exp011 (~0.32, deterministic floor ~0.27) remains the plateau.

- **exp018 ragReasonedFewShot** — quality **0.247** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 3. **DISCARDED** (−0.073 vs exp003/exp011 best ~0.32; −0.060 vs baseline 0.307). Genuinely new lever — in-schema chain-of-thought, untried (every prior config either emitted the 7 questions DIRECTLY with no explicit prioritisation step, or pushed the which-unknown-matters judgment into a SEPARATE FM pass that compounded weak 3B judgment: brainstorm→select exp001, plan→assumptions exp013, auditor exp005, tournament exp012). This is the middle path: a SINGLE call whose output schema forces the model to FIRST fill `criticalUnknowns: [String].count(7)` (short phrases naming the 7 highest-leverage unknowns) and THEN write one question probing each in order — guided generation emits fields in declared order, so the leading list is in-schema CoT that conditions the questions, with NO extra weak-judgment pass. A worked reasoning demo (the unknowns for "Organize my garage") + exp011's contrastive RAG base anchor what good critical-unknown identification looks like (classic CoT few-shot). Hypothesis: forcing the model to NAME which unknowns matter before phrasing would stop it defaulting to generic catch-alls. **Falsified — and it actively regressed, the worst of the RAG-derived family alongside exp013/exp016.** Making the 3B commit to "the 7 most decision-critical unknowns" first does NOT confer the judgment it lacks: the unknowns it names ARE its generic/wrong ones, so conditioning questions on them just hard-wires the wrong coverage. The judge notes are the same recurring gaps verbatim — dinner_party still asks how many friends when the task says 8 and skips dietary + cook-vs-cater; trip_paris still drops departure city (swapped for generic "how do you plan to travel"); buy_used_car still misses make/model/age/financing and wastes slots on insurance/prior-test-drive; resume still misses existing-resume + level; tax stays generic (income/deductions, never residency/Finanzamt); gp misses who-for/urgency/booking-channel and re-asks time-of-day in Q1≈Q7. WORSE, the think-first step REINTRODUCED catastrophic perspective/sanity errors the single-call exp011 avoids: find_therapist Q4 "How much experience do you have as a therapist?" (confuses user with therapist) + Q6/Q7 presuppose specific therapists already identified; resume mixed "I"/"you" and asked from a RECRUITER's POV ("what education do you prefer for a candidate"); bakery phrased as "my bakery" as if the assistant owns it; wedding Q1 phrased as asking the assistant to research venues. So an explicit in-schema reasoning prefix does not supply which-unknown-is-critical knowledge — the model fills the reasoning slot with the same modal generic/confused content and then the questions inherit it, and the added abstraction (name-the-unknown-then-phrase-it) actually degrades naturalness/sanity vs emitting questions directly. This closes the in-schema-CoT / think-first-in-one-call angle too, complementing the multi-call think-first losers (exp001/013). The wall now holds across single-call direct emission, in-schema CoT, every multi-call topology, all post-processing/repair, guided schema, and the full in-context retrieval family. exp003/exp011 (~0.32) remains the plateau; the only genuinely untried lever remains the weight-level LoRA adapter (corpus now 283 sets, adapter/data staged, but no toolkit downloaded and no `.fmadapter` trained/exposed — see autoresearch/ADAPTER_SETUP.md).

- **exp017 ragFillerRepair** — quality **0.282** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed **4**. **DISCARDED** (−0.041 vs exp011 best 0.323; −0.025 vs baseline 0.307). Genuinely new combo on the "filler detect-and-repair" lever: DETERMINISTIC Swift detection of WASTED slots (curated filler-phrase patterns + content-word Jaccard near-dupe pairs) on the exp011 contrastive-RAG draft, then ONE scoped call refilling ONLY those slots with concrete task-specific questions (shown the kept set, told to be specific, given NO universal-dimension checklist), strong slots verbatim + a defensive re-check reverting refills that are themselves filler/dupes. Grounded in exp011's per-case notes where the dominant complaint is not "missed the critical unknown" but that redundant clusters + vague catch-alls "crowd out more valuable questions" (bakery Q1/Q2/Q3/Q7, gp Q3/Q4, guitar Q5/Q7 overlap; "specific features or preferences" filler) — i.e. the model often KNOWS task-specific unknowns but wastes slots, so freeing the wasted ones and refilling concretely *should* recover coverage without needing the missing ranking judgment. Distinct from the two prior repair losers: exp005 (FM auditor injected a UNIVERSAL budget/timeline checklist → wrong-dimension everywhere) and exp009 (embedding gap-finder picked the wrong gold question + re-duplicated). **Falsified — the wall held, the same way every refill/regeneration variant fails.** The deterministic detector DID work mechanically (nonRed held at 4, no catastrophic re-dupe storms), but the REFILL step filled the freed slots with NEW low-value/niche questions rather than the critical unknowns: buy_used_car swapped its catch-alls for test-drive-availability + all-wheel-drive/off-road (still missing age/search-radius/financing); gp got dietary-allergies + insurance-status (still missing urgency/who-for) AND kept the self-defeating "already booked?"; resume got salary + programming-languages + project-timelines (still missing existing-resume/level); trip got "specific destination in Paris" + still-present dietary/transport (still missing departure-city/duration); tax stayed generic (software/goals/deadlines, never residency/Finanzamt/employment-type). Worse, the open-ended "write a concrete specific question" refill REINTRODUCED atomicity violations the single-call base avoids — bakery Q5 bundled "short, easy to remember, AND pronounce", wedding Q1 combined venue AND date, find_therapist Q3 combined "seen one before AND what was your experience" — dropping atomicity to 4 and even adding fresh overlap (find_therapist Q4/Q6 both modality, learn_guitar Q1/Q6 both goals/genres). So freeing the wasted slots does NOT recover coverage: the 3B's REPLACEMENT for a filler/duplicate slot is just ANOTHER non-critical question (a different niche or a new compound), because choosing what concrete-specific to ask in the freed slot is the SAME which-unknown-is-critical judgment it lacks — deterministic detection can find the wasted slot but cannot tell the model what SHOULD go there, and the model's free choice regresses to its modal niche/compound tendencies. This closes the detect-and-repair lever from the deterministic-detection angle too (exp009 closed the embedding angle): no matter how precisely you identify the wasted slot, the 3B refills it with non-critical content. The wall now holds across the ENTIRE prompting/topology/post-processing/repair space; exp003/exp011 (~0.32) remains the plateau; the only genuinely untried lever remains the weight-level LoRA adapter (corpus now 236 sets, adapter/data staged, but no toolkit downloaded and no `.fmadapter` trained/exposed — see autoresearch/ADAPTER_SETUP.md).

- **exp016 ragSequential** — quality **0.245** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 3. **DISCARDED** (−0.075 vs exp003/exp011 best ~0.32; −0.062 vs baseline 0.307). Genuinely new TOPOLOGY (every prior config emitted all 7 in ONE guided emission — flat array, scored pool, tournament winner, or 7 typed slots): generate questions ONE AT A TIME, 8 calls (1 framing + 7), each question-call shown the already-asked set and told to probe a genuinely DIFFERENT unknown, on the exp003 RAG few-shot base. Hypothesis grounded in exp010's datum: the 3B's MODAL output is the generic catch-all and the sharp task-specific unknowns live in the TAIL of its sampling distribution; emitting 7 at once lets the model collapse onto the modal cluster, so forcing each successive emission to be NOVEL relative to prior ones should push generation off the modal cluster into the tail — a different generation DYNAMIC than asking the 3B to select/rank/critique/aggregate (the absolute judgment it lacks). **Falsified, and it FAILED on its OWN promised mechanism.** Forced-novelty conditioning did NOT push into the task-specific tail AND did NOT even buy the non-redundancy it structurally should have: the judge flags SEVERE redundancy in the worst cases — bakery Q5≈Q7 ("name of your current bakery/business" near-identical), tax Q4≈Q6 (both "hire a tax professional") plus Q1/Q3/Q7 all circling document-readiness, find_therapist Q3≈Q7 (concern vs reason for therapy), wedding Q3/Q7 overlap. So even with an explicit numbered do-NOT-repeat list in-context, the 3B re-asks near-duplicates one step later — sequential conditioning does not confer the self-difference tracking it presumes. Worse, generating in isolation made the model lean on LOW-VALUE yes/no STATUS CHECKS to manufacture "novelty" (trip: flights-booked?/itinerary-created?/destination-within-Paris?; wedding: is-a-venue-booked; buy_used_car: have-you-researched-the-market?/maintenance-experience?), which the judge calls "robotic and survey-like," and these crowded out the SAME recurring critical unknowns every config misses: trip omits group size/duration/budget (and departure city), resume omits existing-resume/title/seniority, buy_used_car omits type/age/mileage/search-radius, dinner omits budget + cook-vs-cater (and compounded cuisine+dietary in Q7), tax omits residency/employment-type/Finanzamt, gp omits urgency/who-for. Coverage stayed pinned at 3, naturalness DROPPED to robotic. The mechanism backfired specifically: asking for "the next DIFFERENT unknown" in isolation makes the 3B reach for trivially-different SURFACE variations (status yes/nos, narrower re-asks) rather than genuinely-different DECISION-CRITICAL dimensions, because picking a different *critical* dimension is exactly the which-unknown-is-critical judgment it lacks — relativising it to "different from the prior set" doesn't supply the missing knowledge, and the per-step isolation removes the whole-set view that single-call configs at least use to avoid the worst overlaps (so nonRed regressed vs exp003's 4). This closes the sequential/iterative-decomposition angle too: the wall holds across single-call (×many), and now ALL multi-call topologies — chained brainstorm→select (exp001), best-of-N coverage scorer (exp004), reflexion editor (exp005), pairwise tournament (exp012), plan-assumptions (exp013), self-consistency (exp010), and sequential one-at-a-time (exp016) — every multi-FM variant compounds or fails to escape the 3B's missing which-unknown-is-critical judgment, landing ≤ a single exp003 draw. exp003/exp011 (~0.32) remains the plateau; the only genuinely untried lever remains the weight-level LoRA adapter (corpus has 193 sets and adapter/data is staged, but no `.fmadapter` is trained and no adapter knob is exposed in DiscoveryConfig yet — see autoresearch/ADAPTER_SETUP.md).

- **exp015 ragDimensionalSchema** — quality **0.243** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover **3**, nat 3, nonRed 3. **DISCARDED** (−0.077 vs exp003/exp011 best ~0.32; −0.064 vs baseline 0.307). Genuinely new lever — guided-SCHEMA design, untried (every prior config emitted one flat `questions: [FMQuestion].count(7)` array): replaced the flat array with SEVEN distinctly-`@Guide`d typed slots, one per universal high-value planning dimension the judge keeps citing as MISSING (goal/scope/who-for/budget/timeline/current-state/constraints), on the exp011 RAG+contrastive base. Hypothesis: the 3B can't RANK which unknown is critical and freely DROPS the critical slot for a generic one (exp003-014 all coverage 3); guided generation ENFORCES each named field, so the model structurally CANNOT omit budget/who-for/timeline/current-state — coverage breadth becomes a property of the schema, not of judgment the 3B lacks. **Falsified — it actively regressed, the worst non-pipeline variant.** Forcing FIXED universal dimensions is just exp005/006's mechanical-checklist-injection failure made RIGID: the schema guaranteed breadth of GENERIC dimensions but those are NOT the task-critical unknowns, and the fixed slots CROWDED OUT the task-specific ones. The judge notes are unanimous on this exact mechanism: the BUDGET slot injected irrelevant money questions into bakery-naming, GP-booking, and resume tasks; the WHO-FOR slot produced an "oddly phrased, low-value 'who is the wedding for?'" (weddings are inherently for the couple); the SCOPE slot forced meta/odd asks ("how many concepts to explore?", "how many cars are you considering?"); the CONSTRAINTS slot produced exactly the vague "things you want to avoid" catch-alls the contrastive lesson was meant to suppress; and most damning, the rigid slots REPLACED the actually-critical task-specific unknowns — trip_paris asked about a "booked flight" in the current-state slot INSTEAD OF departure city, find_therapist's fixed dims squeezed out location + in-person-vs-online, buy_used_car's slots omitted make/model/mileage/age/search-area, tax stayed entirely generic (never Finanzamt/residency/employment-type). The schema even reintroduced first-person "I" on resume that exp003 had suppressed. So structural enforcement of a UNIVERSAL dimension set does not confer which-unknown-is-critical judgment — it forces presence of the wrong unknowns at the expense of the right ones; a dimension being universally-common ≠ decision-critical for THIS task. This closes the guided-schema/structural-coverage lever from the universal-dimension angle (a task-conditioned per-slot schema is impossible since `@Guide` is compile-time static, not runtime-dynamic). The wall holds across the ENTIRE prompting/topology/post-processing space now: in-context demos (×6), checklists (×2), selection/aggregation/critique (×4), plan-introspection (×1), and structural schema (×1) all land coverage 2-3 / ~0.31-0.32. exp003/exp011 (~0.32) remains the plateau; the only genuinely untried lever remains weight-level distillation / a LoRA adapter trained on the corpus (no adapter knob is exposed in DiscoveryConfig and no `.fmadapter` is trained yet — see autoresearch/ADAPTER_SETUP.md).

- **exp014 ragCorpusFewShot** — quality **0.280** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 3. **DISCARDED** (−0.040 vs exp003/exp011 best ~0.32; −0.027 vs baseline 0.307). Genuinely new lever per the rules' encouragement to USE the corpus: every prior retrieval variant (exp003/007/008) drew demonstrations from only the 12 generic hardcoded GoldExemplars, so the "nearest" exemplar was frequently OFF-DOMAIN — I hypothesised exp007's "semantically-nearest≠instructive" was really a tiny-impoverished-POOL artifact, not a retrieval-is-useless verdict. So I swapped the BANK (not the mechanism): semantic NLEmbedding retrieval of the k=3 nearest of the 117 corpus/ Sonnet sets (baby-shower→dinner_party, 401k→retirement, visa/Portugal→trip), kept on exp011's contrastive base. **Falsified.** A richer, genuinely close-DOMAIN demonstration bank does NOT confer which-unknown-is-critical judgment either — coverage stayed pinned at 3 and the headline LANDED BELOW baseline, the worst of the positive-RAG family. The judge notes are the SAME recurring gaps verbatim: trip_paris still omits departure city (and swaps in dietary), resume still never asks about an existing resume (and regressed to first-person "I am applying for"/"my current salary"), buy_used_car still drops mileage/age/search-area and bundles make/model/year, wedding still misses guest count + overlaps location/venue, gp wastes slots on payment/dietary and misses urgency/who-for, tax still can't ground in Germany (asks the user to NAME the tax authority). Worse, k=3 close-domain demos slightly INCREASED compound questions (dinner_party Q5, find_therapist Q2/Q5) and reintroduced the first-person phrasing exp003 had suppressed — more/closer demonstrations gave the 3B more surface to imitate without supplying the missing ranking judgment. This definitively closes the "the pool was the limiter" escape hatch: even with 117 specific, well-matched Sonnet exemplars, in-context demonstration plateaus at coverage 3 / ~0.28-0.32. The entire in-context-retrieval family is now exhausted across BOTH the retrieval mechanism (Jaccard/semantic) AND the bank size/specificity (12 generic → 117 specific). exp003/exp011 (~0.32) remains the plateau; the only genuinely untried lever is still weight-level distillation / a LoRA adapter trained on the corpus (no adapter knob is exposed in DiscoveryConfig).

- **exp013 ragPlanAssumptions** — quality **0.245** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover **2**, nat 3, nonRed 3. **DISCARDED** (−0.075 vs exp003 best 0.320; −0.062 vs baseline; coverage 3→2). Genuinely new lever, not selection/aggregation/critique/in-context-demo: stage 1 had the 3B draft a CONCRETE plan and surface the ASSUMPTIONS it was forced to commit to (premise: to write a real plan for "trip to Paris" it must assume departure city/dates/budget/scale = the decision-critical unknowns, grounded in THIS task unlike brainstormSelect's abstract generic listing); stage 2 turned those assumed unknowns into 7 questions via the exp003 RAG few-shot scaffold for phrasing/atomicity. **Falsified — it made coverage WORSE, the worst of any RAG-derived variant.** The mechanism backfired in a specific, instructive way: when a 3B drafts a plan it commits to EXECUTION/LOGISTICS detail (seating arrangement, music DJ-vs-playlist, in-city transport, documentation/paperwork, "do you have folders?"), so the assumptions it surfaces skew to MINOR logistical specifics, not the high-level decision-critical fundamentals (budget, who-for, departure city, duration, scale). The judge notes are unanimous: dinner_party wasted slots on seating/music and MISSED date/venue/budget/cook-vs-cater; trip_paris spent slots on in-city transport/dietary/medical and missed departure-city/duration/accommodation; buy_used_car asked procedural docs/laws/transport and omitted type/make/mileage/age; tax asked "do you have folders?"/"permits or approvals?" (nonsensical for personal tax) and missed employment-status/residency/Finanzamt; gp wasted two slots splitting date+time then asked clinical intake (medications/dietary) for a BOOKING. Worse, the pipeline reintroduced the catastrophic and robotic errors the single-call exp003 had suppressed: "Have you owned a therapist before?" (nonsensical), wedding framed as "do you have access to X?" checklists, resume Q1 in first-person "I". So planning-first does NOT surface decision-critical unknowns — it surfaces the model's logistical-elaboration tendencies, which are exactly the low-value slots gold avoids; and the extra stage re-added the naturalness/sanity failures that motivated the plateau warning that naive multi-FM compounds weak 3B judgment. This closes the plan/counterfactual-assumption decomposition angle too. The wall holds: every route that asks the stock 3B to GENERATE, RANK, SELECT, AGGREGATE, CRITIQUE, or now PLAN-THEN-INTROSPECT its way to which-unknown-is-critical lands at coverage 2-3 / ~0.31-0.32. exp003 (0.320) remains best; the only genuinely untried lever remains weight-level distillation / a LoRA adapter on the gold sets (no adapter knob is currently exposed in DiscoveryConfig).

- **exp012 ragTournament** — quality **0.297** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 4. **DISCARDED** (−0.023 vs exp003 best 0.320; −0.026 vs exp011 0.323; −0.010 vs baseline 0.307). Smart best-of-N as the rules prescribe: drew 4 independent exp003 RAG sets at temps [0.3,0.5,0.7,0.9], then selected via a single-elimination PAIRWISE 3B tournament — each match shows the 3B the task + two whole 7-question sets and asks which is better, judged in BOTH orderings with vote tally (tie→lower-temp incumbent) to damp position bias; winner returned VERBATIM so atomicity/phrasing are never mangled. Hypothesis: exp004 (absolute Swift coverage scorer) and exp010 (frequency clustering) failed because they ask the 3B/heuristics for ABSOLUTE judgment; a 3B is decent at RELATIVE judgment, so a pairwise tournament should pick the coverage-better set and lift best-of-4 above a single draw. **Falsified.** Relative pairwise judgment over the 3B's own samples does NOT recover coverage either — it stayed at 3 and the headline LANDED BELOW a single exp003 draw (0.297 < 0.320), below even baseline. The judge notes on the *winning* sets are the same recurring gaps verbatim, and worse, riddled with the exact off-task questions the comparator prompt was told to penalise: dietary-restriction questions survived in the winners for find_therapist, gp_appointment AND trip_paris; gp_appointment's winner wastes slots on medications/allergies for a *booking* and never asks urgency or booking channel; buy_used_car's winner has a compound Q7 and drops mileage/financing; tax's winner invents a fake authority "Steuereinzugskammer"; dinner_party skips budget; resume skips existing-resume+level+timeline; learn_guitar skips owns-a-guitar+budget; wedding drops the which-areas-need-help scope question. So the tournament selected sets that are no better on coverage than a single draw — the 3B's pairwise verdict is NOT correlated with which set covers the decision-critical unknowns. This makes sense given the wall: ranking two sets by "which covers the most decision-critical unknowns" REQUIRES the same which-unknown-is-critical judgment the 3B lacks; relativising the question doesn't supply the missing knowledge, it just asks for it in comparison form. Position-bias damping (two-order voting) and verbatim return (protecting atomicity; nonRed held at 4) didn't matter because the selection signal itself is noise. This now closes the SELECTION family from all three angles — absolute keyword scoring (exp004), frequency consensus (exp010), and relative pairwise tournament (exp012) — alongside the in-context family. No way to select, aggregate, critique, or contrast the 3B's own generic outputs into coverage it never produced in any sample. The plateau is exp003/exp011 (~0.32); the only genuinely untried lever remains weight-level distillation / a LoRA adapter trained on the gold sets.

- **exp011 ragContrastiveFewShot** — quality **0.323** (dev), spec 100%, 0/1/9. Rubric: atom 5, spec 3, cover 3, nat 3, nonRed 3. **MARGINAL NEW BEST (+0.003 vs exp003 0.320 — within run-to-run noise; treat as a TIE, not a real gain).** Kept exp003's robust single-call positive few-shot and PREPENDED one fixed GOOD-vs-BAD worked example on a neutral non-eval task ("Organize my garage") whose WEAK set demonstrates the exact anti-patterns the judge flags on exp003 — restating given facts, off-task/self-defeating questions, vague filler, compound asks, redundant pairs. Hypothesis: every prior coverage fix added a runtime JUDGING step (auditor/repair/scaffold/consensus) and regressed because the 3B can't judge; baking the judgment into a demonstration CONTRAST (negative few-shot, a lever no prior config used) might teach by example what positive-only demos and instruction-checklists couldn't. **Essentially falsified as a coverage lever.** The rubric means are IDENTICAL to exp003 across all five dimensions and the headline moved only +0.003 (noise). Coverage stayed pinned at 3 and the per-case notes recur verbatim: dinner_party still misses budget+dietary, trip_paris still misses departure city+duration, resume still misses existing-resume, learn_guitar still misses owns-a-guitar, tax still treated as generic document-management with no Germany-specific grounding, bakery still has the type/theme/products/brand overlap cluster. Negative demonstrations of anti-patterns do NOT confer which-unknown-is-decision-critical judgment any more than positive demonstrations (exp003) or explicit checklists (exp005/006) do — the model can recite "don't ask filler" yet still spends slots on secondary concerns (entertainment over budget, modality over who-for) because it cannot rank task-specific unknowns by decision impact. It DID avoid the catastrophic self-defeating question (no "what is the name of your bakery?" this run) and held atomicity at 5, so contrastive demos may marginally suppress the worst off-task/compound errors — but that ceiling is the same 0.32 plateau every in-context variant hits. This now also closes the contrastive/negative-demonstration angle. The entire in-context family (single-shot, brainstorm-select, over-generate-score, RAG positive few-shot ×2 retrievals, checklists ×2, auditor, adapt-exemplar, repair, self-consistency, contrastive) converges at ~0.31-0.32 with coverage stuck at 3. The one remaining untried lever is genuine weight-level distillation / a LoRA adapter trained on the gold sets. exp003/exp011 (~0.32) is the plateau.

- **exp010 ragSelfConsistency** — quality **0.222** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover **2**, nat 3, nonRed 3. **DISCARDED** (−0.098 vs exp003 best 0.320; −0.085 vs baseline). Drew 4 independent exp003 RAG sets at temps [0.4,0.6,0.8,1.0], embedding-clustered all 28 questions (NLEmbedding cosine, greedy, merge≥0.62), kept the 7 clusters with the broadest CROSS-SAMPLE agreement (distinct-sample count; tiebreak earlier avg position), each represented by its centroid-nearest member. Hypothesis: decision-critical unknowns are high-probability → recur across samples, while one-off niche/redundant slots don't → consensus would dedupe AND surface the critical unknowns (a NEW criticality signal vs exp002's self-rated importance and exp004's whole-set keyword scoring). **Falsified two ways.** (1) The dedup didn't happen: NLEmbedding sentence cosine does NOT reliably co-cluster paraphrases of the same unknown — "What date is the party?" vs "What time will it start?" fall below 0.62 — so the SAME near-duplicate pairs survived (dinner Q1/Q2 both date; trip Q2/Q4 party-size + Q3/Q6 accommodation; wedding Q1/Q2 date; find_therapist/learn_guitar/gp all kept overlapping pairs). Embeddings can rank exemplar relevance (exp007) but are too coarse to merge clarifying-question paraphrases. (2) Worse, cross-sample frequency surfaced FILLER, not criticality: the questions that recur most across samples are the model's high-probability GENERIC catch-alls ("any additional factors?", "any specific requirements or preferences?", "what software are you using?") — exactly the low-value slots gold avoids — so consensus CONCENTRATES the 3B's generic tendencies and discards the occasional sharp task-specific question only one sample produced. Merging across samples also mixed phrasings, reintroducing pronoun drift (wedding "we/us" vs "you"). Coverage fell to 2. New-angle confirmation of the wall: you cannot recover which-unknowns-matter judgment by aggregating more of the 3B's own samples — its MODAL output IS the generic one. exp003 (0.320) remains best; the only untried lever remains weight-level distillation / a LoRA adapter on the gold sets.

- **exp009 ragCoverageRepair** — quality **0.213** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed **2**. **DISCARDED** (−0.107 vs exp003 best 0.320; −0.094 vs baseline — the worst RAG variant yet). Idea: keep exp003's strong draft, then move the coverage JUDGMENT out of the 3B entirely — in Swift via NLEmbedding, find the concrete gold question (from the 2 nearest exemplars) whose unknown the draft covers LEAST, find the most-redundant draft slot, and do ONE focused FM call adapting that gold question into the redundant slot (6/7 questions untouched). The hope was a surgical, low-risk coverage patch that can't mangle the draft. **Falsified — and it actively introduced redundancy.** The single failure mode dominates the notes: the repair call produced near/exact DUPLICATES of an already-present draft question in ~8/10 cases (bakery Q3=Q6 verbatim, find_therapist Q1=Q4 verbatim, gp Q3=Q4, learn_guitar Q1≈Q7, trip_paris Q2=Q4 verbatim, resume Q1≈Q2, wedding Q3≈Q4, tax "deadlines or deadlines"), tanking nonRed 4→2. Despite the explicit "do NOT duplicate these" list and replacing the *most-redundant* slot, the 3B regenerated content it already had — and the embedding gap-detector mis-targets too: the "least-covered" gold question is often a niche dimension (dietary for a GP appointment, dietary for trip planning) that, when adapted, adds noise rather than the actually-missing high-value unknown (budget/who-for/departure-city). So a deterministic embedding ruler picks the wrong gap, and a one-shot single-question FM call can't reliably avoid restating an existing question. Coverage stayed 3 — the swap didn't even buy coverage, it just cost non-redundancy. Confirms the exp003-008 pattern from a new angle: even when coverage judgment is removed from the 3B and made deterministic, the FM *generation* of the patch reintroduces the redundancy the draft had avoided, and embedding-similarity is too blunt to identify *which* unknown is decision-critical (it finds the most-dissimilar gold question, not the most-important-missing one). The over-generate-then-deterministically-select family is now also exhausted alongside retrieval/checklist/whole-set-adapt. exp003 (0.320) remains best; the only untried lever remains weight-level distillation / a LoRA adapter on the gold sets.

- **exp008 ragAdaptExemplar** — quality **0.247** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 3. **DISCARDED** (−0.073 vs exp003 best 0.320; −0.060 vs baseline). Made the nearest exemplar's 7 CONCRETE gold questions hard constraints: model must adapt each one-to-one to the new task, preserving the unknown each probes, so gold's dimension SPAN transfers directly while the 3B does only the surface rewrite. This was the heavier lever the log kept pointing at ("force it to keep/adapt N specific gold questions"). **Falsified — and it actively regressed.** The 3B does NOT preserve "the underlying unknown" of each exemplar slot; it anchors on surface and drifts, producing MORE redundancy than exp003 (trip_paris asked dates 3× in Q1/Q2/Q5; gp_appointment had 3 overlapping scheduling Qs; wedding emitted a literal "any specific vendors or vendors in mind?" duplicate/typo), and it still reintroduced classic filler/catch-alls ("Do you have any questions or concerns?", "Is there anything else I should know?" on tax). Coverage stayed 3 — the dimension span did NOT transfer: dinner_party still missed dietary + cook-vs-cater, learn_guitar still missed current skill level, resume still missed existing-resume, trip still missed departure city. It even surfaced a placeholder title artifact ("FMDiscoveryPlan") on bakery. So the strongest RAG-injection variant — concrete questions as one-to-one hard constraints — performs WORSE than just showing them as free demonstrations (exp003). This closes the loop on the entire RAG/prompt-injection family (exp003-008): retrieval relevance (007), checklists universal (005) or retrieved (006), best-of-N selection (004), and now concrete-question hard constraints (008) all leave coverage pinned at 3. The 3B cannot be prompted into which-unknowns-matter judgment from any amount of in-context gold; the only untried lever left is genuine weight-level distillation / a LoRA adapter trained on the gold sets. exp003 (0.320) remains best.

- **exp007 ragFewShotSemantic** — quality **0.273** (dev), spec 100%, 0/0/10. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 3. **DISCARDED** (−0.047 vs exp003 best 0.320; also lost exp003's lone learn_guitar tie). Replaced exp003's word-overlap Jaccard exemplar retrieval with on-device NLEmbedding sentence-embedding cosine, so the 2 few-shot demonstrations are the nearest task TYPE even with zero shared vocabulary (resume↔interview, tax↔budget). Hypothesis was that exp003's win came from well-matched exemplars and that Jaccard's ~0 matches on topically distinct dev queries were leaving quality on the table. **Falsified.** Better-matched exemplars did NOT improve coverage — it stayed flat at 3, and the same failure modes recur verbatim (misses budget/departure-city/who-for, treats tax as document-filing, asks "do you have a budget?" instead of "what is your budget?", produces near-duplicate date pairs in wedding/trip). The 3B does not *transfer* an exemplar's decision-critical unknowns even when the exemplar is a closer semantic match; it imitates surface form, not which-unknowns-matter judgment. So exp003's edge was NOT about exemplar topical relevance (and swapping retrieval slightly hurt, plausibly because semantically-nearest≠most-instructive, e.g. a finance exemplar for tax steered toward generic money questions). Confirms again: retrieval/prompt-quality levers don't move the coverage wall. The remaining untried levers are the heavier ones the log keeps pointing to — forcing the model to keep/adapt N specific concrete gold questions as hard constraints, or distillation/adapter — not anything that merely changes what context we show a single FM call.

- **exp006 ragCoverageScaffold** — quality **0.290** (dev), spec 100%, 0/1/9. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 3. **DISCARDED** (−0.030 vs exp003 best 0.320). Tagged each gold question with a short decision-critical dimension label; at runtime aggregated the dimensions from the SAME 2 nearest exemplars exp003 retrieves and injected them as an explicit, adaptable, task-conditioned coverage checklist in a single call (no auditor). Insight: a RETRIEVED checklist is no better than a universal one — coverage stayed 3 and the headline dropped. The 3B treats abstract dimension labels ("budget", "scale", "things to avoid") as slot-fillers and turns them into VAGUE catch-all or COMPOUND questions: it rendered "scale"→generic "how many" re-asks, bundled labels into double-barreled asks (wedding "date AND time", "started planning AND hired vendors"), produced near-duplicate pairs (tax filed-vs-received), and even repeated "deadlines or deadlines". Naming the dimension does not give the 3B the judgment to (a) pick which dimension is most critical for THIS task or (b) phrase it atomically — it just adds an extra surface to over-fit. Both exp005 (universal checklist) and exp006 (retrieved checklist) confirm: explicit dimension menus, universal OR task-conditioned, do NOT fix coverage. The 3B needs the actual decision-critical QUESTION modeled, not a label — pointing toward injecting concrete retrieved exemplar questions as harder constraints (e.g. require it to keep/adapt N specific gold questions), or distillation/adapter, rather than any checklist scaffold.

- **exp005 ragCritiqueRevise** — quality **0.315** (dev), spec 100%, 0/1/9. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 3. **DISCARDED** (−0.005 vs exp003 best 0.320). Reflexion editor on the RAG draft: stage-1 = exp003, stage-2 = a 2nd FM auditor given the draft + a decision-critical checklist (budget/who-for/timeline/scale/location/current-state/goal), told to delete already-given/low-value/compound questions and fill the highest-value missing unknown, keeping strong questions verbatim. Insight: a hard universal checklist BACKFIRES on coverage — the auditor mechanically injected budget+timeline into tasks where they're noise (bakery naming got budget+timeline slots; gp_appointment got an irrelevant budget Q; tax got "storage budgets"), and the 3B can't tell which checklist dimensions are task-appropriate. It also introduced new redundancy (overlapping reworded pairs) and a literal "deadlines or deadlines" repetition. The editor neither reliably dropped the *right* given facts nor added the *right* missing unknown; it just swapped one set of wrong slots for another. Coverage stayed 3. Self-critique still can't supply the task-type judgment the 3B lacks; the checklist must be task-conditioned (retrieved per task type), not universal — pointing back toward richer RAG/distillation rather than reflexion.

- **exp004 ragCoverageBestOfN** — quality **0.245** (dev), spec 90% (1 error), 0/0/9. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 3. **DISCARDED** (−0.075 vs exp003 best; −0.062 vs baseline). Sampled 4 RAG sets at temps [0.3,0.6,0.9,1.0] and selected the best by a deterministic Swift coverage scorer (weighted dimension span: budget/timeline/scale/location/current-state high, redundancy + "how many"-already-stated penalties). Insight: best-of-N on a coverage-only ruler BACKFIRES — high-temp samples degrade atomicity (compound/parenthetical questions: dinner_party bundled budget+dietary, trip_paris parenthetical sub-asks → spec error) and specificity (tax got generic US W-2/1099 framing), and a keyword-coverage scorer can't see those and selects the worse set. Coverage did NOT improve (still 3). A useful ruler must score atomicity+specificity+task-fit, not just dimension keyword presence; and sampling noise needs a low-temp floor. Coverage gap remains the wall; selection-by-proxy on the 3B's own outputs is insufficient.

- **exp003 ragFewShot** — quality **0.320** (dev), spec 100%, 0/1/9. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 4. **NEW BEST** (+0.013 vs baseline). Hard-coded 12 non-dev gold exemplars; at runtime retrieve 2 nearest by word-overlap Jaccard, inject as few-shot demonstrations in system prompt, single FM call. Non-redundancy improved 3→4; first tie vs gold (learn_guitar). Judge notes show model still makes catastrophic errors (asks "What is the name of your bakery?" for a naming task; asks redundant scheduling questions for GP appointment); critical coverage gaps persist (budget, departure city, group size). The RAG scaffolding helped non-redundancy but the 3B still can't reliably reason about which unknowns are most decision-critical.

- **PIVOT (after EXP-002):** Both structural moves lost to single-shot on the
  dev proxy. exp002 improved non-redundancy (3→4 via score+select-in-Swift) but
  collapsed coverage (3→2); pipelines also hurt naturalness (3rd-person phrasing).
  Judge notes are unanimous across configs: the gap is **coverage** — the 3B
  can't reliably identify the most decision-critical unknown per task type.
  Instructions/topology don't move it. Pivoting to distillation levers:
  (6) retrieval-augmented few-shot from the gold bank, then (7) adapter. This
  matches the prior real-world finding that prompt/pipeline tweaks were not
  sufficient. Best so far: **baseline 0.307**.

- **EXP-002 overGenerateScore** (dev) — quality **0.263**, spec 100%, 0/0/10.
  Rubric: atom 5, spec 3, cover 2, nat 3, nonRed 4. One call → 12 scored
  candidates → top-7 by importance in Swift. Non-redundancy best of all configs;
  coverage worst — importance scores rank niche features above critical unknowns.
- **EXP-001 brainstormSelect** (dev) — quality **0.235**, spec 90% (1 error),
  0/0/9. Rubric: atom 4, spec 3, cover 3, nat 3, nonRed 3. Two-stage; regressed
  vs single-shot, added robotic 3rd-person phrasing in the select stage.
- **baseline singleShot** (dev) — quality **0.307**, spec 100%, 0/1/9. Rubric:
  atom 5, spec 3, cover 3, nat 3, nonRed 3. (Full-30: 0.282, see EXP-000.)

- **EXP-001 dimension-scaffolded pipeline** — pending. Hypothesis: the gap is
  judgment (which 7 unknowns matter), not phrasing. Decompose into two FM calls
  the 3B can each handle: (1) brainstorm ~12 candidate unknowns against a
  universal planning-dimension scaffold (goal, who-for, scale, budget, timeline,
  location, current-state, resources-owned, constraints, channel, DIY-vs-help);
  (2) select the 7 most decision-relevant, drop redundant/filler/premature ones,
  phrase each as a natural atomic question. Target: beat 0.282; win/tie some.

- **EXP-000 baseline** — QUALITY **0.282**, spec pass 100%, vs gold **0 win /
  0 tie / 30 loss**. Rubric means: atomicity 4, specificity 3, coverage 3,
  naturalness 3, nonRedundancy 3. Single-shot Sonnet-prompt port. Sonnet wins
  every case. Judge-note failure patterns (results/EXP-000.json):
  (1) misses the single most critical unknown (coverage); (2) wastes slots on
  niche/premature detail; (3) redundant overlapping questions; (4) generic
  filler ("any concerns?"); (5) logical/off-target questions (bakery: "what's
  your business name?"); (6) robotic first-person phrasing. Atomicity is the one
  strong dimension — guided generation + the no-and/or rule hold.

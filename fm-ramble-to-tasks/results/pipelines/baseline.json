{
  "summary": "Single-shot greedy baseline extraction",
  "hypothesis": "A plain single-shot greedy call on the stock FM can reliably split rambles into actionable tasks. The expected failure mode is zero-task inputs and retractions, where the model may fabricate or fail to suppress tasks.",
  "technique": "A single greedy on-device FM call using the `singleShot` prompt and a flat `@Generable` task-list schema \u2014 no reasoning chain, no gating, no second pass.",
  "result": "Quality 0.824 on dev-17 (W/T/L vs Sonnet = 0/5/9); all rubric dimensions (faith, atom, action, cover, nonRed) scored 5. KEPT as new best. The result confirms extraction cases are strong but the gap to Sonnet is driven by zero-task failures and a retraction miss, consistent with the known weaknesses of a flat schema.",
  "stages": [
    {
      "kind": "input",
      "text": "User free-form ramble received"
    },
    {
      "kind": "generate",
      "text": "Single-shot greedy task extraction",
      "knobs": {
        "sampling": "greedy"
      },
      "prompt": "System:\nYou take a free-form brain-dump from the user and extract the distinct, actionable tasks in it.\n\nThe input is stream-of-consciousness text (often dictated). It may contain:\n- ZERO tasks (venting, musing, or thinking out loud with nothing actionable)\n- ONE task\n- SEVERAL distinct tasks, sometimes interleaved (the user jumps back to an earlier thought)\n\nRULES:\n- Each task is a short, actionable one-liner in the user's own terms (around 4-9 words).\n- ONE intention = ONE task. If the user mentions the same thing more than once, output it ONCE.\n- Only extract things the user actually wants to DO. A vague wish or feeling is NOT a task.\n- NEVER invent tasks. Every task must trace directly to something in the input.\n- If nothing in the input is an actionable task, return an empty list. Do not force a task.\n- Do NOT use emojis. Keep text clean and professional.\n\nUser:\nHere is what the user brain-dumped:\n\n\"\\(input)\"\n\nExtract each distinct, actionable task as a short one-liner. If there are none, return an empty list."
    },
    {
      "kind": "output",
      "text": "Final 0..N tasks returned"
    }
  ],
  "label": "baseline"
}
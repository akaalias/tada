- prog005 COVERAGE base (exhaustive candidate sweep + same zero-task gate) under prog004's aligned evidence-first restyle finisher, from prog004+prog004 — fitness 0.766 (full), NEW BEST (+0.031 over elite, edge of the ~0.04 noise band but every signal coherent): the swap did exactly what prog004's log predicted — RECALL jumped, F1 0.882→0.940 (interleaved_party f1 0.50→tie, multi_car/multi_holidays/multi_moving now ties, recall 0.944). phrasing held at 4, pairwise 0W/11T/12L, zero-task still 100%. The terse under-extracting reasoned base was the shared root of both gaps; the fuller coverage base both raised recall AND handed the restyle richer material. Residuals: (1) long_monday STILL misses the insurance-company call and the "by Thursday" deadline — a buried task even the sweep dropped; (2) interleaved_deck still COLLAPSES the add-Q3-numbers + email-Sarah pair into one slot (alignment guard can't split a merge the base itself made); (3) NEW over-completeness — the restyle now sometimes appends rationale as if it were the task title (noisy_filler "...for my prescription is pretty old", single_plumber "it has been dripping for days"), a phrasing regression on otherwise-correct sets. Next lever: either trim trailing-rationale clauses in the restyle/post-process, or attack the deck-merge + long_monday miss at the base (sweep granularity).
- prog004 prog003 evidence-first restyle + per-slot ALIGNMENT guard (anchor styled slot to its own base task >= half content stems; reject any content stem unique to a DIFFERENT base task) from prog002+prog003 — fitness 0.735 (full), NEW BEST: strictly dominates both parents — F1 0.882 (prog002 0.877, prog003 0.842, scramble FIXED), phrasing 4, pairwise 12T/10L (prog002 8T/13L). The alignment guard let prog003's bolder restyle keep its completeness wins (multi_moving ph5 restored "with the post office"/"to the landlord"/"this weekend"; multi_holidays tie) WITHOUT the multi-task slot scramble that sank prog003's F1. Margin +0.025 over elite is within the ~0.04 noise band but every signal is coherent. Residual: simple list inputs (multi_car/multi_morning ph2) still come back terse — the base extracts clipped fragments AND the restyle echoes them; and base under-extraction (long_monday insurance call, interleaved_report figure-check) still loses pairwise — both are EXTRACTION gaps, not restyle. Next lever: lift completeness/recall in the base reasoned extraction itself (or a coverage base) so the restyle has fuller material to style.
- prog003 prog002 elite + EVIDENCE-FIRST restyle (quote each task's subject/recipient/deadline/location verbatim before rewriting) from prog002 — fitness 0.702 (full), DISCARDED (-0.008, F1 regressed 0.877→0.842). The evidence step DID lift the target gap (phrasing 3→4, pairwise 8T/13L→12T/10L — completeness restores fired: "about the dripping", "for the holidays", "annual") but made the restyle BOLDER than prog002's, and on MULTI-task lists it SCRAMBLED slot content — collapsing 3 distinct tasks into one compound string and duplicating it across slots (multi_car, multi_errands, interleaved_deck/report). Root cause: the token-subset guard only blocks hallucinated WORDS, not cross-slot merge/duplication, since every word in a compounded slot is still input-present. Next lever: a per-slot ALIGNMENT guard (reject a styled slot whose content diverges from ITS own base task / detect cross-slot dup) so completeness can fire WITHOUT set drift.
# fm-ramble-to-tasks — program log

Newest first. One line per program. The autonomous agent prepends here each run.

**Elite:** none yet — fresh start (zero programs). Build the first program
(`prog001`) from the stock `baseline` (single-shot, greedy).

You are scored on the DEV split. The TEST split is held out — never tune toward it.

**Metric:** fitness = 0.5·F1 + 0.25·rubric + 0.25·pairwise (zero-task cases scored binary).
The rubric includes PHRASING. Gold: 35 cases (27 dev / 8 test).

**State of the gap (to be discovered — nothing run yet):**
- ZERO-TASK is the known #1 failure mode — do NOT invent tasks on non-actionable input
  (venting / musing / retractions); an empty list is the correct answer.
- PHRASING is expected to be the hard part: match Sonnet's capitalized, conversational,
  complete style (keep detail) rather than terse all-lowercase fragments — without losing F1.

## Log
- prog002 elite reasoned extract + decoupled F1-safe verbatim restyle finisher (downcase+recase+completeness prompt) from prog001 — fitness 0.710 (full), NEW BEST (marginal, +0.019 within noise but every signal coherent): phrasing 2→3, pairwise 5T/17L→8T/13L, F1 flat 0.877 (set membership preserved by 1:1 guard, as designed). Residual: the token-subset guard still REJECTS many fuller restyles back to terse base (drops "with the post office"/"about the kitchen sink"), so completeness under-fires — next lever is loosening the guard for input-verbatim detail without re-opening hallucination. Minor: one restyle attached input-present detail to the wrong task (interleaved_deck), F1 unaffected.
- prog001 reasoning-first zero-task gate (singleShotReasoned, greedy) from baseline — fitness 0.691 (full), NEW BEST (first program), zero-task gate works (100% correct empties, precision 0.926); confirmed gaps now = PHRASING (2/5, terse all-lowercase fragments dropping detail like "with the post office"/"before Thursday") and recall on interleaved many-task rambles (missed buried prerequisites). Pairwise 0 win / 5 tie / 17 loss.

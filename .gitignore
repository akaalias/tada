.build/
.env

# Xcode per-user state (window layouts, breakpoints, scheme prefs, etc.)
xcuserdata/
*.xcuserstate
.DS_Store

# On-device discovery adapter weights (127MB) — bundled at build time from a
# local copy. Track via Git LFS for release builds; the corpus/ is tracked.
Tada/OnDeviceModels/discovery_v2a_e1.fmadapter/adapter_weights.bin
